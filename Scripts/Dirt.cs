using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Dirt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // this.type = GatherableType.Dirt;
    }

    // Update is called once per frame
    void Update()
    {   
        
    }
}
